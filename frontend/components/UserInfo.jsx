import React from 'react';
import AuthComponents from './AuthComponents';

const UserInfo = () => {
    return (
        <div>
            <AuthComponents />
        </div>
    );
};

export default UserInfo;
